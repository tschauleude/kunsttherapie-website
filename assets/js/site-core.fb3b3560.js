/**
 * Übersetzungen DE / EN – Schlüssel mit data-i18n am HTML-Element.
 */
window.I18N_MESSAGES = {
  de: {
    'a11y.skip': 'Zum Inhalt springen',
    'brand.name': 'Kunsttherapie Paderborn',
    'brand.subtitle': 'Psychosoziale Kunsttherapie',
    'nav.ansatz': 'Mein Ansatz',
    'nav.therapy': 'Kunsttherapie',
    'nav.practice': 'Das Atelier',
    'nav.news': 'Neuigkeiten',
    'nav.booking': 'Termin',
    'nav.contact': 'Kontakt',
    'nav.atelier': 'Mini-Atelier',
    'nav.events': 'Veranstaltungen',
    'nav.aria': 'Hauptnavigation',
    'nav.menu': 'Menü',
    'lang.switch': 'Sprache wählen',
    'lang.hint': 'Sprache',
    'footer.practice': 'Atelier',
    'footer.contact': 'Kontakt',
    'footer.offer': 'Angebot',
    'footer.legal': 'Rechtliches',
    'footer.role': 'Psychosoziale & Klinische Kunsttherapeutin',
    'footer.prices': 'Preise',
    'footer.atelier': 'Live-Atelier',
    'footer.imprint': 'Impressum',
    'footer.privacy': 'Datenschutz',
    'footer.tagline': 'Kunsttherapie Paderborn',
    'footer.emailLabel': 'E-Mail schreiben',
    'form.name': 'Name',
    'form.email': 'E-Mail',
    'form.message': 'Nachricht (optional)',
    'form.phone': 'Telefon (optional)',
    'btn.book': 'Termin buchen',
    'btn.intro': 'Kostenloses Kennenlernen',
    'btn.learnMore': 'Mehr erfahren',
    'btn.less': 'Weniger',
    'btn.close': 'Schließen',
    'btn.inquiry': 'Anfrage stellen',
    'btn.contact': 'Kontakt aufnehmen',
    'btn.message': 'Nachricht schicken',
    'home.hero.kicker': 'Kunsttherapie · Paderborn',
    'home.hero.title': 'Kunsttherapie – Dienstag morgens & Donnerstag abends',
    'home.hero.titleHtml': 'Psychosoziale Kunsttherapie in <span class="text-accent">Paderborn</span>',
    'home.hero.lead': 'Wenn Worte nicht reichen, findet das Gestalten einen Weg.',
    'home.hero.sub':
      'Dienstag vormittags und Donnerstag abends in Paderborn – für Privatpersonen, Teams und klinische Einrichtungen.',
    'home.hero.note':
      'Neue Gruppen ab 1. Juli 2026 · Di 11:00–12:30 · Do 18:00–19:30 · Otto-Stadler-Straße 23c',
    'home.audience.title': 'Für wen ist das Angebot?',
    'home.audience.privat': 'Privat',
    'home.audience.privat.desc': 'Gruppe, Auszeit oder Einzel – wenn Worte nicht reichen und Ressourcen gesucht werden.',
    'home.audience.firmen': 'Unternehmen',
    'home.audience.firmen.desc': 'Teambuilding und kreative Teamtage – Verbindung statt Konkurrenz.',
    'home.audience.klinik': 'Kliniken & Einrichtungen',
    'home.audience.klinik.desc': 'Gruppen, Palliativbegleitung, Vorträge – langjährige Klinikerfahrung.',
    'home.times.di': 'Di 11:00–12:30',
    'home.times.do': 'Do 18:00–19:30',
    'home.services.title': 'Zwei Wege – gleich wichtig',
    'home.services.di.title': 'Dienstag morgens · Gruppen & Therapie',
    'home.services.di.text':
      'Gruppentherapie, freies und thematisches Gestalten – begleitetes Malen, Collage und Zeichnen. Die Gruppenzeiten rotieren: im Wechsel auch donnerstags abends.',
    'home.services.do.title': 'Donnerstag abends · Auszeit',
    'home.services.do.text':
      'Kreativabende für Freunde und Familie – gemeinsame Auszeit mit Freude an neuen Erfahrungen. Nicht einzeln buchbar, Termine auf Anfrage.',
    'home.trust.title': 'Erfahrung & Vertrauen',
    'home.trust.text':
      'Über 16 Jahre in Palliativversorgung, Psychoonkologie und klinischen Gruppen – u. a. MZG und Brüderkrankenhaus. Jetzt mein eigenes Atelier in Paderborn.',
    'home.proof.title': 'Was Teilnehmende erleben',
    'home.proof.sub':
      'Stimmen aus Gruppen und Auszeiten (Beispiele – echte, freigegebene Rückmeldungen folgen).',
    'home.proof.1.text':
      'Ich kam mit dem Gefühl, nicht malen zu können – und ging mit einem Bild, das mehr sagte als Worte.',
    'home.proof.1.author': 'Teilnehmerin, Gruppe Dienstag',
    'home.proof.2.text':
      'Ein geschützter Raum, in dem ich einfach sein durfte. Genau das hat mir gefehlt.',
    'home.proof.2.author': 'Teilnehmer, Auszeit',
    'home.proof.3.text':
      'Als Team haben wir uns ganz neu erlebt – ohne Konkurrenz, mit echter Verbindung.',
    'home.proof.3.author': 'Teamleitung, Firmen-Event',
    'home.proof.partners':
      'Langjährige klinische Erfahrung u. a. mit MZG und Brüderkrankenhaus Paderborn.',
    'home.services.priceHint':
      'Gruppensitzung 90 Min · 4–12 Personen · ab 55 € · Material inkl.',
    'quotes.1.text':
      '„Ohne die Freiheit des Geistes ist die Kunst weder machbar, denkbar, noch erfahrbar.“',
    'quotes.1.author': 'Georg Meistermann',
    'quotes.2.text':
      '„Kunst, Kultur, Natur und alles, was sich nicht rechnet, bereichern das Leben, lassen ahnen, wie heil sein ist.“',
    'quotes.2.author': 'Else Pannek',
    'quotes.nav': 'Zitate wechseln',
    'home.gallery.title': 'Kunsttherapie in Aktion',
    'home.praxis.title': 'Atelier in Paderborn – ab 1. Juli 2026',
    'kt.hero.kicker': 'Ab 1. Juli 2026 in Paderborn',
    'kt.hero.title': 'Kunst, die <span class="text-accent">trägt</span> – nicht nur schmückt',
    'kt.hero.sub':
      'Dienstag 11:00–12:30 Uhr und Donnerstag 18:00–19:30 Uhr – feste Atelierzeiten. Die Gruppenzeiten rotieren: im Wechsel auch donnerstags abends.',
    'kt.facts.di': 'Di 11:00–12:30 · Gruppen & Therapie',
    'kt.facts.do': 'Do 18:00–19:30 · Auszeit & Workshops',
    'kt.facts.meta': '90 Min · 4–12 Personen · ab 55 €',
    'kt.audience': 'Für Unternehmen · für Kliniken & Einrichtungen · für Experten & Interessierte',
    'kt.offers.title': 'Welches Angebot passt?',
    'kt.offers.sub': 'Fünf Angebote – ausführliche Beschreibung per Klick.',
    'kt.offers.kicker': 'Formate',
    'offer.1.title': 'Gruppentherapie',
    'offer.1.teaser': 'Begleitetes Malen in der Gruppe – dienstags vormittags.',
    'offer.1.body':
      '<p><strong>Primär dienstags 11:00–12:30 Uhr</strong> – die Gruppenzeiten rotieren und werden im Wechsel auch donnerstags abends angeboten. Viele Menschen kennen bereits kunsttherapeutisches Gestalten in Gruppen, haben die Kunsttherapie schätzen gelernt, Wissen um die Wirksamkeit – und suchen einen freien Platz. Selbstverständlich auch für alle, die durch ressourcenorientierte, visuelle Methoden ganz individuelle Themen gestalten möchten – mit professioneller Begleitung.</p><p><strong>Gruppentherapie, freies und thematisches Gestalten:</strong> Kunst als Weg zurück zum Inneren – um Gesundheit zu erhalten und zu fördern, im Sinne von Resilienz, Krankheitsakzeptanz und psychischer Widerstandsfähigkeit.</p><p>„Bilder sagen mehr als tausend Worte“, sowie „die Heilkraft der Farben“ sind Erfahrungswerte in der Kunsttherapie.</p><p class="note">4–12 Personen · 90 Minuten · im Wechsel auch donnerstags abends</p>',
    'offer.2.title': 'Auszeit: Kreativabende für Freunde & Familien',
    'offer.2.teaser': 'Kreativabende für Freunde & Familie – ohne Leistungsdruck.',
    'offer.2.body':
      '<p><strong>Donnerstag 18:00–19:30 Uhr.</strong> Viele sagen danach: „Hätte nicht gedacht, dass mir ein Bild so gut gelingt.“</p><p>Buchbar für Familie und Freunde – <strong>nicht einzeln buchbar</strong>. Es geht um Freude durch neue Erfahrungen und eine gute kreative gemeinsame Auszeit.</p><p class="note">Termine auf Anfrage</p>',
    'offer.3.title': 'Einzelsitzungen',
    'offer.3.teaser': 'Intensiv und persönlich – flexibel nach Vereinbarung.',
    'offer.3.body':
      '<p>Intensiv und persönlich: psychosoziale und klinische Kunsttherapie in Krisen, bei Trauer oder in der Krankheitsverarbeitung – individuell auf die jeweilige Situation abgestimmt.</p><p>Im geschützten Rahmen des Ateliers oder nach Vereinbarung. Termine flexibel außerhalb der Gruppenzeiten – Preis auf Anfrage.</p>',
    'offer.4.title': 'Teambuilding & Institutionen',
    'offer.4.teaser': 'Raus aus dem Büro, gemeinsam ein Werk gestalten.',
    'offer.4.body':
      '<p>Raus aus dem Büro, rein ins FarbenReich – Kolleginnen und Kollegen MALend ganz neu erleben. Es entsteht ein Werk mit Überraschungseffekt, mit spielerischen Regeln und echter Verbindung, das wertschätzend einen exponierten Platz in der Firma bekommen darf.</p><p class="note">Mindestens 12, maximal 18 Teilnehmer. Bei mehr als 18 Teilnehmern bitte Kontakt aufnehmen. Termine nach Absprache.</p>',
    'offer.5.title': 'Vorträge & Impulse',
    'offer.5.teaser': 'Impulse aus über 16 Jahren klinischer Kunsttherapie.',
    'offer.5.body':
      '<p>Regelmäßige Vorträge und Impulse aus der Kunsttherapie.</p><p><strong>Warum Kunst statt nur Gespräch?</strong> Ein Einblick in Wirkung, Methoden und Erfahrungen aus über 16 Jahren klinischer Arbeit – für Einrichtungen, Fachpublikum und Interessierte. Auch Gruppen (z.&nbsp;B. Senioren) und palliative Begleitung aus langjähriger Praxis.</p><p class="note">Dauer, Ort und Honorar nach Absprache – Konzept und Material inklusive.</p>',
    'atelier.kicker': "Ausprobieren",
    'atelier.title': 'Mini-Atelier – einfach loslegen',
    'atelier.intro':
      'Malen, Linien und Formen setzen, Fotos zur Kollage legen. Optional kann das Werk später an das Atelier gesendet werden.',
    'atelier.hint': 'Hier starten …',
    'atelier.tools': 'Werkzeuge',
    'atelier.tool.brush': 'Pinsel',
    'atelier.tool.line': 'Linie',
    'atelier.tool.rect': 'Rechteck',
    'atelier.tool.circle': 'Oval',
    'atelier.tool.move': 'Verschieben',
    'atelier.color': 'Farbe',
    'atelier.size': 'Stärke',
    'atelier.fill': 'Fläche füllen',
    'atelier.undo': 'Rückgängig',
    'atelier.redo': 'Wiederholen',
    'atelier.save': 'Speichern',
    'atelier.load': 'Laden',
    'atelier.clear': 'Leeren',
    'atelier.upload': 'Bild hochladen (Kollage)',
    'atelier.remove': 'Bild entfernen',
    'atelier.send': 'An das Atelier senden',
    'atelier.sendNote': 'Das Werk wird als Bild übermittelt und vertraulich genutzt.',
    'atelier.anonymous': 'Anonym senden',
    'atelier.submit': 'Werk absenden',
    'atelier.back': 'Zurück zu Kunsttherapie & Angeboten',
  },
  en: {
    'a11y.skip': 'Skip to content',
    'brand.name': 'Art Therapy Paderborn',
    'brand.subtitle': 'Psychosocial Art Therapy',
    'nav.ansatz': 'Approach',
    'nav.therapy': 'Art Therapy',
    'nav.practice': 'Atelier',
    'nav.news': 'News',
    'nav.booking': 'Booking',
    'nav.contact': 'Contact',
    'nav.atelier': 'Mini Studio',
    'nav.events': 'Events',
    'nav.aria': 'Main navigation',
    'nav.menu': 'Menu',
    'lang.switch': 'Choose language',
    'lang.hint': 'Language',
    'footer.practice': 'Atelier',
    'footer.contact': 'Contact',
    'footer.offer': 'Services',
    'footer.legal': 'Legal',
    'footer.role': 'Psychosocial & Clinical Art Therapist',
    'footer.prices': 'Prices',
    'footer.atelier': 'Live Studio',
    'footer.imprint': 'Imprint',
    'footer.privacy': 'Privacy',
    'footer.tagline': 'Art Therapy Paderborn',
    'footer.emailLabel': 'Send e-mail',
    'form.name': 'Name',
    'form.email': 'Email',
    'form.message': 'Message (optional)',
    'form.phone': 'Phone (optional)',
    'btn.book': 'Book appointment',
    'btn.intro': 'Free intro call',
    'btn.learnMore': 'Learn more',
    'btn.less': 'Less',
    'btn.close': 'Close',
    'btn.inquiry': 'Send inquiry',
    'btn.contact': 'Get in touch',
    'btn.message': 'Send message',
    'home.hero.kicker': 'Art Therapy · Paderborn',
    'home.hero.title': 'Art therapy – Tuesday mornings & Thursday evenings',
    'home.hero.titleHtml': 'Art therapy in <span class="text-accent">Paderborn</span>',
    'home.hero.lead': 'When words fall short, creating finds a way.',
    'home.hero.sub':
      'Tuesday mornings and Thursday evenings in Paderborn – for individuals, teams, and clinical settings.',
    'home.hero.note':
      'New groups from 1 July 2026 · Tue 11:00–12:30 · Thu 18:00–19:30 · Otto-Stadler-Straße 23c',
    'home.audience.title': 'Who is this for?',
    'home.audience.privat': 'Individuals',
    'home.audience.privat.desc': 'Group, downtime, or one-to-one – when words are not enough.',
    'home.audience.firmen': 'Companies',
    'home.audience.firmen.desc': 'Team building and creative off-sites – connection instead of competition.',
    'home.audience.klinik': 'Clinics & institutions',
    'home.audience.klinik.desc': 'Groups, palliative care, talks – years of clinical experience.',
    'home.times.di': 'Tue 11:00–12:30',
    'home.times.do': 'Thu 18:00–19:30',
    'home.services.title': 'Two paths – equally important',
    'home.services.di.title': 'Tuesday mornings · Groups & therapy',
    'home.services.di.text':
      'Group therapy, free and themed creating – guided painting, collage, and drawing. Group times rotate: on alternate weeks also on Thursday evenings.',
    'home.services.do.title': 'Thursday evenings · Downtime',
    'home.services.do.text':
      'Creative evenings for friends and family – shared downtime and new experiences together. Not bookable individually; dates on request.',
    'home.trust.title': 'Experience & trust',
    'home.trust.text':
      'Over 16 years in palliative care, psycho-oncology, and clinical groups – including MZG and Brüderkrankenhaus. Now my own atelier in Paderborn.',
    'home.proof.title': 'What participants experience',
    'home.proof.sub':
      'Voices from groups and creative evenings (examples – real, approved feedback to follow).',
    'home.proof.1.text':
      'I arrived sure I couldn\'t paint – and left with a picture that said more than words could.',
    'home.proof.1.author': 'Group participant, Tuesday',
    'home.proof.2.text':
      'A safe space where I could simply be. That\'s exactly what I had been missing.',
    'home.proof.2.author': 'Participant, creative evening',
    'home.proof.3.text':
      'As a team we saw each other in a whole new way – no competition, real connection.',
    'home.proof.3.author': 'Team lead, corporate event',
    'home.proof.partners':
      'Years of clinical experience, including MZG and Brüderkrankenhaus Paderborn.',
    'home.services.priceHint':
      'Group session 90 min · 4–12 people · from €55 · materials incl.',
    'quotes.1.text':
      '“Without freedom of mind, art is neither possible, conceivable, nor experienceable.”',
    'quotes.1.author': 'Georg Meistermann',
    'quotes.2.text':
      '“Art, culture, nature, and everything that cannot be counted enrich life and give a sense of how healing being is.”',
    'quotes.2.author': 'Else Pannek',
    'quotes.nav': 'Change quote',
    'home.gallery.title': 'Art therapy in action',
    'home.praxis.title': 'Atelier in Paderborn – from 1 July 2026',
    'kt.hero.kicker': 'From 1 July 2026 in Paderborn',
    'kt.hero.title': 'Art that <span class="text-accent">supports</span> – not just decorates',
    'kt.hero.sub':
      'Tuesday 11:00–12:30 and Thursday 18:00–19:30 – fixed studio hours. Group times rotate: on alternate weeks also on Thursday evenings.',
    'kt.facts.di': 'Tue 11:00–12:30 · Groups & therapy',
    'kt.facts.do': 'Thu 18:00–19:30 · Downtime & workshops',
    'kt.facts.meta': '90 min · 4–12 people · from €55',
    'kt.audience': 'For companies · for clinics & institutions · for experts & interested visitors',
    'kt.offers.title': 'Which offer fits?',
    'kt.offers.sub': 'Five services – click for the full description.',
    'kt.offers.kicker': 'Formats',
    'offer.1.title': 'Group therapy',
    'offer.1.teaser': 'Guided group painting – Tuesday mornings.',
    'offer.1.body':
      '<p><strong>Primarily Tuesday 11:00–12:30</strong> – group times rotate and are also offered on Thursday evenings on alternate weeks. Many people already know art therapy in groups, value its effectiveness – and are looking for an open place. Also for anyone who wants to work on individual themes through resource-oriented visual methods – with professional support.</p><p><strong>Group therapy, free and themed creating:</strong> art as a path inward – to maintain and support health, in the sense of resilience, illness acceptance, and psychological strength.</p><p>“A picture is worth a thousand words” and “the healing power of colour” are lived experiences in art therapy.</p><p class="note">4–12 people · 90 minutes · on alternate weeks also Thursday evenings</p>',
    'offer.2.title': 'Downtime: creative evenings for friends & family',
    'offer.2.teaser': 'Creative evenings for friends & family – no pressure to perform.',
    'offer.2.body':
      '<p><strong>Thursday 18:00–19:30.</strong> Many say afterwards: “I never thought I could create something like that.”</p><p>Bookable for family and friends – <strong>not individually</strong>. About joy through new experiences and a good shared creative break.</p><p class="note">Dates on request</p>',
    'offer.3.title': 'One-to-one sessions',
    'offer.3.teaser': 'Intensive and personal – flexible by arrangement.',
    'offer.3.body':
      '<p>Intensive and personal: psychosocial and clinical art therapy in crisis, grief, or illness – tailored to each situation.</p><p>In the atelier or by arrangement. Flexible times outside group slots – price on request.</p>',
    'offer.4.title': 'Team building & institutions',
    'offer.4.teaser': 'Out of the office – create a shared artwork together.',
    'offer.4.body':
      '<p>Out of the office, into a world of colour – experience colleagues in a whole new, creative way. A shared artwork with a surprise effect, playful rules, and real connection – worthy of a visible place in the company.</p><p class="note">Minimum 12, maximum 18 participants. For more than 18, please get in touch. Dates by arrangement.</p>',
    'offer.5.title': 'Talks & impulses',
    'offer.5.teaser': 'Impulses from over 16 years of clinical art therapy.',
    'offer.5.body':
      '<p>Regular talks and impulses from art therapy.</p><p><strong>Why art instead of conversation alone?</strong> An insight into methods, effects, and experience from over 16 years of clinical work – for institutions, professionals, and interested audiences. Including groups (e.g. seniors) and palliative support from long practice.</p><p class="note">Duration, venue, and fee by arrangement – concept and materials included.</p>',
    'atelier.kicker': 'Try the mini studio',
    'atelier.title': 'Mini studio – start creating',
    'atelier.intro':
      'Paint, draw shapes, add photo collages. Artwork can optionally be sent to the atelier.',
    'atelier.hint': 'Start here …',
    'atelier.tools': 'Tools',
    'atelier.tool.brush': 'Brush',
    'atelier.tool.line': 'Line',
    'atelier.tool.rect': 'Rectangle',
    'atelier.tool.circle': 'Oval',
    'atelier.tool.move': 'Move',
    'atelier.color': 'Color',
    'atelier.size': 'Size',
    'atelier.fill': 'Fill shape',
    'atelier.undo': 'Undo',
    'atelier.redo': 'Redo',
    'atelier.save': 'Save',
    'atelier.load': 'Load',
    'atelier.clear': 'Clear',
    'atelier.upload': 'Upload image (collage)',
    'atelier.remove': 'Remove image',
    'atelier.send': 'Send to the atelier',
    'atelier.sendNote': 'Artwork is sent as an image and treated confidentially.',
    'atelier.anonymous': 'Send anonymously',
    'atelier.submit': 'Submit artwork',
    'atelier.back': 'Back to art therapy & services',
  },
};

;
/**
 * Seiteninhalt per Selektor → i18n-Schlüssel (ergänzt data-i18n im HTML).
 */
window.I18N_PAGE_BINDINGS = {
  home: [
    { sel: '#newsPopupTitle', key: 'home.popup.title', attr: 'text' },
    { sel: '#newsPopup .news-popup-kicker', key: 'home.news.kicker', attr: 'text' },
    { sel: '#newsPopup .news-popup-actions a.btn.outline', key: 'home.news.all', attr: 'text' },
    { sel: '#newsPopup .news-popup-actions button.btn.primary', key: 'btn.close', attr: 'text' },
    { sel: '#neuigkeiten .section-intro .kicker', key: 'home.news.kicker', attr: 'text' },
    { sel: '#neuigkeiten .section-intro h2', key: 'nav.news', attr: 'text' },
    { sel: '#neuigkeiten .section-intro .sub', key: 'home.news.sub', attr: 'text' },
    { sel: '#homeNewsLoading p', key: 'home.news.loading', attr: 'text' },
    { sel: '#openNewsPopupBtn', key: 'home.news.show', attr: 'text' },
    { sel: '#neuigkeiten .center a.btn.outline', key: 'home.news.all', attr: 'text' },
    { sel: '#galerie .section-intro .kicker', key: 'home.gallery.kicker', attr: 'text' },
    { sel: '#galerie .section-intro h2', key: 'home.gallery.title', attr: 'text' },
    { sel: '#galerie .section-intro .sub', key: 'home.gallery.sub', attr: 'text' },
    { sel: '#angebote .section-intro .kicker', key: 'home.services.kicker', attr: 'text' },
    { sel: '#atelier .section-intro .kicker', key: 'kt.praxis.kicker', attr: 'text' },
    { sel: '#atelier .section-intro h2', key: 'home.praxis.title', attr: 'text' },
    { sel: '#atelier .section-intro .sub', key: 'home.praxis.sub', attr: 'text' },
    { sel: '#atelier .twoCol > div:first-child > p:first-child', key: 'home.praxis.address', attr: 'html' },
    { sel: '#atelier .twoCol > div:first-child > p:nth-of-type(2)', key: 'home.praxis.di', attr: 'html' },
    { sel: '#atelier .twoCol > div:last-child > p:first-child', key: 'home.praxis.do', attr: 'html' },
    { sel: '#atelier .twoCol .note', key: 'home.praxis.link', attr: 'html' },
    { sel: '#atelier .actions .btn.primary', key: 'btn.book', attr: 'text' },
    { sel: '#atelier .actions .btn.outline', key: 'home.praxis.more', attr: 'text' },
    { sel: '.hero-right--landing img', key: 'home.hero.imgAlt', attr: 'alt' },
  ],
  therapy: [
    { sel: '.page-kunsttherapie .actions .btn.outline', key: 'btn.inquiry', attr: 'text' },
    { sel: '#angebote .section-intro .kicker', key: 'kt.offers.kicker', attr: 'text' },
    { sel: '#atelier .section-intro .kicker', key: 'kt.praxis.kicker', attr: 'text' },
    { sel: '#atelier .section-intro h2', key: 'kt.praxis.title', attr: 'text' },
    { sel: '#atelier .section-intro .sub', key: 'kt.praxis.sub', attr: 'html' },
    { sel: '[data-raum-panel="eingang"] .kt-raum-hint', key: 'kt.raum.compare.hint', attr: 'text' },
    { sel: '[data-raum-panel="innen"] .kt-raum-hint', key: 'kt.raum.innen.hint', attr: 'text' },
    { sel: '[data-raum-panel="gestalten"] .kt-raum-hint', key: 'kt.raum.gestalten.hint', attr: 'text' },
    { sel: '[data-raum-tab="eingang"] span:last-child', key: 'kt.raum.tab.eingang', attr: 'text' },
    { sel: '[data-raum-tab="innen"] span:last-child', key: 'kt.raum.tab.innen', attr: 'text' },
    { sel: '[data-raum-tab="gestalten"] span:last-child', key: 'kt.raum.tab.gestalten', attr: 'text' },
    { sel: '.kt-raum-compare-labels span:first-child', key: 'kt.raum.compare.before', attr: 'text' },
    { sel: '.kt-raum-compare-labels span:last-child', key: 'kt.raum.compare.after', attr: 'text' },
    { sel: '.kt-praxis-copy > h2', key: 'kt.praxis.headline', attr: 'text' },
    { sel: '.kt-praxis-copy > p:nth-of-type(1)', key: 'kt.praxis.p1', attr: 'html' },
    { sel: '.kt-praxis-copy > p:nth-of-type(2)', key: 'kt.praxis.p2', attr: 'html' },
    { sel: '.kt-schedule li:nth-child(1)', key: 'kt.praxis.schedule1', attr: 'html' },
    { sel: '.kt-schedule li:nth-child(2)', key: 'kt.praxis.schedule2', attr: 'html' },
    { sel: '.kt-praxis-copy .actions .btn.primary', key: 'kt.praxis.book', attr: 'text' },
    { sel: '.kt-praxis-copy .actions .btn.outline', key: 'kt.praxis.news', attr: 'text' },
    { sel: '.kt-praxis-highlight:nth-child(1) h3', key: 'kt.highlight1.title', attr: 'text' },
    { sel: '.kt-praxis-highlight:nth-child(1) p', key: 'kt.highlight1.text', attr: 'text' },
    { sel: '.kt-praxis-highlight:nth-child(2) h3', key: 'kt.highlight2.title', attr: 'text' },
    { sel: '.kt-praxis-highlight:nth-child(2) p', key: 'kt.highlight2.text', attr: 'text' },
    { sel: '.kt-praxis-highlight:nth-child(3) h3', key: 'kt.highlight3.title', attr: 'text' },
    { sel: '.kt-praxis-highlight:nth-child(3) p', key: 'kt.highlight3.text', attr: 'text' },
    { sel: '.kt-praxis-highlight:nth-child(4) h3', key: 'kt.highlight4.title', attr: 'text' },
    { sel: '.kt-praxis-highlight:nth-child(4) p', key: 'kt.highlight4.text', attr: 'html' },
    { sel: '.kt-location-grid .card:first-child h3', key: 'kt.location.title', attr: 'text' },
    { sel: '.kt-location-grid .card:first-child > p:first-of-type', key: 'kt.location.address', attr: 'html' },
    { sel: '.kt-location-grid .card:first-child .note', key: 'kt.location.phone', attr: 'html' },
    { sel: '.kt-location-grid .card:first-child p:last-child a:first-child', key: 'btn.message', attr: 'text' },
    { sel: '.kt-location-grid .card:first-child p:last-child a:last-child', key: 'footer.prices', attr: 'text' },
    { sel: '.map-placeholder p', key: 'map.consent', attr: 'html' },
    { sel: '[data-enable-maps]', key: 'map.show', attr: 'text' },
    { sel: '.map-placeholder .btn.outline', key: 'map.open', attr: 'text' },
    { sel: '.kt-map-wrap .note a', key: 'map.route', attr: 'text' },
    { sel: '.kt-cta-band h2', key: 'kt.cta.title', attr: 'text' },
    { sel: '.kt-cta-band .sub', key: 'kt.cta.sub', attr: 'text' },
    { sel: '.kt-cta-band .btn.primary', key: 'kt.cta.book', attr: 'text' },
    { sel: '.kt-cta-band .btn.outline', key: 'kt.cta.about', attr: 'text' },
    { sel: '.kt-cta-band .note', key: 'kt.cta.news', attr: 'html' },
    { sel: '[data-raum-panel="innen"] .kt-raum-stage img', key: 'kt.praxis.imgAlt', attr: 'alt' },
    { sel: '[data-raum-hotspot]:nth-of-type(1) .kt-hotspot-tip strong', key: 'kt.raum.hotspot1.title', attr: 'text' },
    { sel: '[data-raum-hotspot]:nth-of-type(1) .kt-hotspot-tip span', key: 'kt.raum.hotspot1.text', attr: 'text' },
    { sel: '[data-raum-hotspot]:nth-of-type(2) .kt-hotspot-tip strong', key: 'kt.raum.hotspot2.title', attr: 'text' },
    { sel: '[data-raum-hotspot]:nth-of-type(2) .kt-hotspot-tip span', key: 'kt.raum.hotspot2.text', attr: 'text' },
    { sel: '[data-raum-panel="innen"] [data-raum-hotspot]:nth-of-type(1) .kt-hotspot-tip strong', key: 'kt.raum.hotspot3.title', attr: 'text' },
    { sel: '[data-raum-panel="innen"] [data-raum-hotspot]:nth-of-type(1) .kt-hotspot-tip span', key: 'kt.raum.hotspot3.text', attr: 'text' },
    { sel: '[data-raum-panel="innen"] [data-raum-hotspot]:nth-of-type(2) .kt-hotspot-tip strong', key: 'kt.raum.hotspot4.title', attr: 'text' },
    { sel: '[data-raum-panel="innen"] [data-raum-hotspot]:nth-of-type(2) .kt-hotspot-tip span', key: 'kt.raum.hotspot4.text', attr: 'text' },
  ],
  about: [
    { sel: '.page-ueber-mich h1', key: 'about.name', attr: 'text' },
    { sel: '.page-ueber-mich .about-role', key: 'about.kicker', attr: 'text' },
    { sel: '.page-ueber-mich .about-prose > p:not(.about-role)', key: 'about.intro', attr: 'text' },
    { sel: '.about-journey h2', key: 'about.journey.title', attr: 'text' },
    { sel: '.about-journey p:nth-of-type(1)', key: 'about.journey.p1', attr: 'text' },
    { sel: '.about-journey p:nth-of-type(2)', key: 'about.journey.p2', attr: 'text' },
    { sel: '.about-passion h2', key: 'about.passion.title', attr: 'text' },
    { sel: '.about-passion p:nth-of-type(1)', key: 'about.passion.p1', attr: 'text' },
    { sel: '.about-passion p:nth-of-type(2)', key: 'about.passion.p2', attr: 'text' },
    { sel: '.about-quote', key: 'about.quote', attr: 'text' },
    { sel: '.about-qual h2', key: 'about.qual.title', attr: 'text' },
    { sel: '.about-qual-list', key: 'about.qual.list', attr: 'html' },
    { sel: '.page-ueber-mich .actions .btn.primary', key: 'btn.contact', attr: 'text' },
    { sel: '.page-ueber-mich .actions .btn.outline', key: 'about.offers', attr: 'text' },
    { sel: '.hero-portrait-frame img', key: 'about.imgAlt', attr: 'alt' },
  ],
  booking: [
    { sel: '.booking-page .kicker', key: 'book.kicker', attr: 'text' },
    { sel: '.booking-page h1', key: 'book.title', attr: 'text' },
    { sel: '.booking-page .page-hero .sub', key: 'book.sub', attr: 'html' },
    { sel: '#prevMonth', key: 'book.prevMonth', attr: 'aria-label' },
    { sel: '#nextMonth', key: 'book.nextMonth', attr: 'aria-label' },
    { sel: '.booking-legend', key: 'book.legend', attr: 'html' },
    { sel: '.booking-weekdays', key: 'book.weekdays', attr: 'html' },
    { sel: '#calendarGrid', key: 'book.calendarAria', attr: 'aria-label' },
    { sel: '#selectedDayLabel', key: 'book.pickDay', attr: 'text' },
    { sel: '#bookingFormPanel h2', key: 'book.formTitle', attr: 'text' },
    { sel: 'label[for="bookName"]', key: 'form.name', attr: 'text' },
    { sel: 'label[for="bookEmail"]', key: 'form.email', attr: 'text' },
    { sel: 'label[for="bookPhone"]', key: 'form.phone', attr: 'text' },
    { sel: 'label[for="bookMessage"]', key: 'form.message', attr: 'text' },
    { sel: '#bookMessage', key: 'book.messagePlaceholder', attr: 'placeholder' },
    { sel: '#bookingForm .note', key: 'book.privacyNote', attr: 'html' },
    { sel: '#bookSubmit', key: 'book.submit', attr: 'text' },
    { sel: '.booking-noscript-fallback h2', key: 'book.noscript.title', attr: 'text' },
    { sel: '.booking-noscript-fallback .sub', key: 'book.noscript.text', attr: 'text' },
    { sel: '.booking-noscript-fallback li:nth-of-type(1) strong', key: 'book.noscript.phone', attr: 'text' },
    { sel: '.booking-noscript-fallback li:nth-of-type(2) strong', key: 'book.noscript.mobile', attr: 'text' },
    { sel: '.booking-noscript-fallback li:nth-of-type(3) strong', key: 'book.noscript.email', attr: 'text' },
  ],
  contact: [
    { sel: '.page-hero .kicker', key: 'contact.kicker', attr: 'text' },
    { sel: '.page-hero h1', key: 'contact.title', attr: 'text' },
    { sel: '.page-hero .sub', key: 'contact.sub', attr: 'html' },
    { sel: '.contact-card h2:first-of-type', key: 'contact.reach.title', attr: 'text' },
    { sel: '.contact-list', key: 'contact.reach.list', attr: 'html' },
    { sel: '.contact-card .note', key: 'contact.hours', attr: 'text' },
    { sel: '.contact-card .actions .btn.primary', key: 'contact.book', attr: 'text' },
    { sel: '.contact-card .actions .btn.outline', key: 'contact.offers', attr: 'text' },
    { sel: '.contact-layout .contact-card:last-child h2', key: 'contact.form.title', attr: 'text' },
    { sel: 'label[for="contactName"]', key: 'form.name', attr: 'text' },
    { sel: 'label[for="contactEmail"]', key: 'form.email', attr: 'text' },
    { sel: 'label[for="contactPhone"]', key: 'form.phone', attr: 'text' },
    { sel: 'label[for="contactMessage"]', key: 'contact.form.messageLabel', attr: 'text' },
    { sel: '#contactMessage', key: 'contact.messagePlaceholder', attr: 'placeholder' },
    { sel: '#contactForm .note', key: 'contact.privacyNote', attr: 'html' },
    { sel: '#contactForm button[type="submit"]', key: 'contact.submit', attr: 'text' },
    { sel: '#anfahrt-heading', key: 'contact.map.title', attr: 'text' },
    { sel: '.map-card .map-placeholder p', key: 'map.consent', attr: 'html' },
    { sel: '.map-card [data-enable-maps]', key: 'map.show', attr: 'text' },
    { sel: '.map-card .map-placeholder .btn.outline', key: 'map.open', attr: 'text' },
    { sel: '.map-card iframe', key: 'contact.mapIframeTitle', attr: 'title' },
  ],
  prices: [
    { sel: 'main .kicker', key: 'prices.kicker', attr: 'text' },
    { sel: 'main h1', key: 'prices.title', attr: 'text' },
    { sel: 'main > .card > .sub', key: 'prices.sub', attr: 'text' },
    { sel: 'table.prices thead', key: 'prices.tableHead', attr: 'html' },
    { sel: 'table.prices tbody', key: 'prices.tableBody', attr: 'html' },
    { sel: 'main .prices-on-request', key: 'prices.onRequest', attr: 'text' },
    { sel: 'main .prices-unit', key: 'prices.unit', attr: 'text' },
    { sel: 'main > .card > .note', key: 'prices.note', attr: 'text' },
    { sel: 'main .actions .btn.primary', key: 'btn.book', attr: 'text' },
    { sel: 'main .actions .btn.outline', key: 'prices.ask', attr: 'text' },
  ],
  news: [
    { sel: 'main .kicker', key: 'newsPage.kicker', attr: 'text' },
    { sel: 'main h1', key: 'newsPage.title', attr: 'text' },
    { sel: 'main .sub', key: 'newsPage.sub', attr: 'text' },
    { sel: '#newsLoading p', key: 'newsPage.loading', attr: 'text' },
    { sel: '#newsEmpty p', key: 'newsPage.empty', attr: 'text' },
  ],
  events: [
    { sel: 'main .kicker', key: 'eventsPage.kicker', attr: 'text' },
    { sel: 'main h1', key: 'eventsPage.title', attr: 'text' },
    { sel: 'main .sub', key: 'eventsPage.sub', attr: 'text' },
    { sel: '#eventsLoading p', key: 'eventsPage.loading', attr: 'text' },
    { sel: '#eventsEmpty p:first-child', key: 'eventsPage.empty', attr: 'text' },
    { sel: '#eventsEmpty p:last-child', key: 'eventsPage.emptyHint', attr: 'text' },
  ],
  imprint: [{ sel: 'main .legal-prose', key: 'legal.imprint.body', attr: 'html' }],
  privacy: [{ sel: 'main .legal-prose', key: 'legal.privacy.body', attr: 'html' }],
};

;
/**
 * Zweisprachigkeit DE / EN – data-i18n, data-i18n-html, Meta, Platzhalter.
 */
(function () {
  if (document.body.classList.contains('admin-app')) return;

  const STORAGE_KEY = 'kt-lang';
  const DEFAULT_LANG = 'de';

  function getMessages() {
    return window.I18N_MESSAGES || { de: {}, en: {} };
  }

  async function loadOverrides() {
    try {
      const res = await fetch('/api/i18n/overrides', { credentials: 'same-origin' });
      if (!res.ok) return;
      const data = await res.json();
      if (!window.I18N_MESSAGES) window.I18N_MESSAGES = { de: {}, en: {} };
      ['de', 'en'].forEach((lang) => {
        if (!data[lang] || typeof data[lang] !== 'object') return;
        Object.assign(window.I18N_MESSAGES[lang], data[lang]);
      });
    } catch (_) {
      /* offline / static hosting without API */
    }
  }

  function detectLang() {
    const stored = localStorage.getItem(STORAGE_KEY);
    if (stored === 'de' || stored === 'en') return stored;
    const browser = (navigator.language || '').toLowerCase();
    if (browser.startsWith('en')) return 'en';
    return DEFAULT_LANG;
  }

  let currentLang = detectLang();

  function t(key) {
    const msgs = getMessages()[currentLang] || {};
    if (msgs[key] != null) return msgs[key];
    const fallback = getMessages().de[key];
    return fallback != null ? fallback : null;
  }

  function localeTag() {
    return currentLang === 'en' ? 'en-GB' : 'de-DE';
  }

  function applyMeta() {
    const page = document.body.dataset.i18nPage;
    if (!page) return;

    const title = t(`meta.${page}.title`);
    const desc = t(`meta.${page}.description`);
    if (title) document.title = title;
    if (desc) {
      const meta = document.querySelector('meta[name="description"]');
      if (meta) meta.setAttribute('content', desc);
      document.querySelectorAll('meta[property="og:description"], meta[name="twitter:description"]').forEach((el) => {
        el.setAttribute('content', desc);
      });
    }
    if (title) {
      document.querySelectorAll('meta[property="og:title"], meta[name="twitter:title"]').forEach((el) => {
        el.setAttribute('content', title);
      });
    }

    const ogLocale = document.querySelector('meta[property="og:locale"]');
    if (ogLocale) ogLocale.setAttribute('content', currentLang === 'en' ? 'en_GB' : 'de_DE');
  }

  function applyTranslations() {
    document.documentElement.lang = currentLang === 'en' ? 'en' : 'de';

    document.querySelectorAll('[data-i18n]').forEach((el) => {
      const key = el.getAttribute('data-i18n');
      const val = t(key);
      if (val == null) return;
      if (el.tagName === 'INPUT' || el.tagName === 'TEXTAREA') {
        if (el.hasAttribute('placeholder')) el.placeholder = val;
        else el.value = val;
      } else if (el.tagName === 'A' && el.querySelector('[data-i18n]')) {
        /* Link text lives in nested data-i18n span */
      } else {
        el.textContent = val;
      }
    });

    document.querySelectorAll('[data-i18n-html]').forEach((el) => {
      const key = el.getAttribute('data-i18n-html');
      const val = t(key);
      if (val != null) el.innerHTML = val;
    });

    document.querySelectorAll('[data-i18n-title]').forEach((el) => {
      const key = el.getAttribute('data-i18n-title');
      const val = t(key);
      if (val != null) el.title = val;
    });

    document.querySelectorAll('[data-i18n-placeholder]').forEach((el) => {
      const key = el.getAttribute('data-i18n-placeholder');
      const val = t(key);
      if (val != null) el.placeholder = val;
    });

    document.querySelectorAll('[data-i18n-aria-label]').forEach((el) => {
      const key = el.getAttribute('data-i18n-aria-label');
      const val = t(key);
      if (val != null) el.setAttribute('aria-label', val);
    });

    document.querySelectorAll('[data-i18n-alt]').forEach((el) => {
      const key = el.getAttribute('data-i18n-alt');
      const val = t(key);
      if (val != null) el.setAttribute('alt', val);
    });

    document.querySelectorAll('.lang-switch-btn[data-lang]').forEach((btn) => {
      const code = btn.getAttribute('data-lang');
      btn.textContent = code ? code.toUpperCase() : btn.textContent;
      const active = code === currentLang;
      btn.setAttribute('aria-pressed', active ? 'true' : 'false');
      btn.classList.toggle('is-active', active);
    });

    applyMeta();
    applyPageBindings();
    if (typeof window.ktInitEmailLinks === 'function') window.ktInitEmailLinks();
    document.dispatchEvent(new CustomEvent('kt-lang-change', { detail: { lang: currentLang } }));
  }

  function applyPageBindings() {
    const page = document.body.dataset.i18nPage;
    const bindings = window.I18N_PAGE_BINDINGS?.[page];
    if (!bindings || !bindings.length) return;

    bindings.forEach(({ sel, key, attr }) => {
      const val = t(key);
      if (val == null) return;
      document.querySelectorAll(sel).forEach((el) => {
        if (el.hasAttribute('data-i18n') || el.hasAttribute('data-i18n-html')) return;
        if (attr === 'html') el.innerHTML = val;
        else if (attr === 'placeholder') el.placeholder = val;
        else if (attr === 'aria-label') el.setAttribute('aria-label', val);
        else if (attr === 'alt') el.setAttribute('alt', val);
        else el.textContent = val;
      });
    });
  }

  function setLang(lang) {
    if (lang !== 'de' && lang !== 'en') return;
    currentLang = lang;
    localStorage.setItem(STORAGE_KEY, lang);
    applyTranslations();
    if (window.ktUpdateToggleLabels) window.ktUpdateToggleLabels();
  }

  window.ktI18n = {
    t,
    setLang,
    getLang: () => currentLang,
    getLocale: localeTag,
    apply: applyTranslations,
  };

  async function boot() {
    await loadOverrides();
    applyTranslations();
    if (typeof window.ktInitLangSwitch === 'function') {
      window.ktInitLangSwitch();
    }
    if (typeof window.ktApplySiteChrome === 'function') {
      window.ktApplySiteChrome();
    }
  }

  function scheduleBoot() {
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', () => {
        boot();
      });
      return;
    }
    /* Body-Skripte (z. B. site.js) laufen erst danach – einen Tick warten */
    window.setTimeout(boot, 0);
  }

  scheduleBoot();
})();

;
/**
 * Strukturierte Daten (Schema.org) für lokale Suche – WebPage/WebSite (LocalBusiness statisch im HTML).
 */
(function () {
  if (document.body.classList.contains('admin-app')) return;

  const origin = 'https://kunsttherapie.mkmpb.de';
  const path = window.location.pathname.replace(/\/$/, '') || '/';
  const pageUrl = origin + (path === '/' ? '/' : path);
  const lang = document.documentElement.lang === 'en' ? 'en-GB' : 'de-DE';
  const ogImage = origin + '/assets/img/Gruppen-und-Einzeltherapie-768x524.jpg';
  const localBusinessId = origin + '/#localbusiness';

  const webPage = {
    '@context': 'https://schema.org',
    '@type': 'WebPage',
    '@id': pageUrl + '#webpage',
    url: pageUrl,
    name: document.title,
    description:
      document.querySelector('meta[name="description"]')?.getAttribute('content') || undefined,
    isPartOf: { '@id': origin + '/#website' },
    about: { '@id': localBusinessId },
    inLanguage: lang,
    primaryImageOfPage: {
      '@type': 'ImageObject',
      url: ogImage,
    },
  };

  const website = {
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    '@id': origin + '/#website',
    url: origin + '/',
    name: 'Kunsttherapie Paderborn',
    description: 'Website des Kunsttherapie-Ateliers in Paderborn',
    inLanguage: ['de-DE', 'en-GB'],
    publisher: { '@id': localBusinessId },
  };

  const graph = {
    '@context': 'https://schema.org',
    '@graph': [website, webPage],
  };

  const script = document.createElement('script');
  script.type = 'application/ld+json';
  script.id = 'kt-schema-org';
  script.textContent = JSON.stringify(graph);
  document.head.appendChild(script);

  document.addEventListener('kt-lang-change', function (e) {
    const nextLang = e.detail?.lang === 'en' ? 'en-GB' : 'de-DE';
    try {
      const data = JSON.parse(script.textContent);
      const page = data['@graph']?.find((n) => n['@type'] === 'WebPage');
      if (page) page.inLanguage = nextLang;
      script.textContent = JSON.stringify(data);
    } catch (_) {
      /* ignore */
    }
  });
})();

;
/**
 * DSGVO: Einwilligung für externe Dienste (Google Maps, Google Fonts).
 * Notwendig: Einstellungen + Neuigkeiten-Hinweis (localStorage, keine Tracking-Cookies).
 */
(function () {
  const STORAGE_KEY = 'kunsttherapie_consent_v1';
  const FONT_URL =
    'https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700&display=swap';

  let fontsLoaded = false;

  function getConsent() {
    try {
      const raw = localStorage.getItem(STORAGE_KEY);
      if (!raw) return null;
      return JSON.parse(raw);
    } catch (e) {
      return null;
    }
  }

  function saveConsent(partial) {
    const next = {
      v: 1,
      necessary: true,
      external: false,
      updated: Date.now(),
      ...getConsent(),
      ...partial,
    };
    localStorage.setItem(STORAGE_KEY, JSON.stringify(next));
    applyConsent(next);
    window.dispatchEvent(new CustomEvent('consent-updated', { detail: next }));
    hideBanner();
    hideSettings();
  }

  function hasChosen() {
    return Boolean(getConsent());
  }

  window.hasExternalConsent = function hasExternalConsent() {
    const c = getConsent();
    return Boolean(c && c.external);
  };

  window.openCookieSettings = function openCookieSettings() {
    showSettings();
  };

  function loadFonts() {
    if (fontsLoaded) return;
    const link = document.createElement('link');
    link.rel = 'stylesheet';
    link.href = FONT_URL;
    document.head.appendChild(link);
    document.documentElement.classList.add('fonts-loaded');
    fontsLoaded = true;
  }

  function applyMaps(enabled) {
    document.querySelectorAll('[data-google-maps-src]').forEach((iframe) => {
      const wrap = iframe.closest('.map-consent-wrap');
      const placeholder = wrap?.querySelector('[data-map-placeholder]');
      if (enabled) {
        if (!iframe.src) iframe.src = iframe.dataset.googleMapsSrc;
        iframe.hidden = false;
        if (placeholder) placeholder.hidden = true;
      } else {
        iframe.removeAttribute('src');
        iframe.hidden = true;
        if (placeholder) placeholder.hidden = false;
      }
    });
  }

  function applyConsent(consent) {
    if (consent.external) {
      loadFonts();
      applyMaps(true);
    } else {
      applyMaps(false);
    }
    syncToggleInputs(consent);
  }

  function syncToggleInputs(consent) {
    const ext = document.getElementById('consentExternal');
    if (ext) ext.checked = Boolean(consent.external);
  }

  let bannerResizeObserver = null;

  function updateBannerOffset() {
    const b = document.getElementById('consentBanner');
    const visible = Boolean(b && !b.hidden);
    if (!visible) {
      document.body.classList.remove('consent-banner-visible');
      document.body.style.removeProperty('--consent-banner-offset');
      bannerResizeObserver?.disconnect();
      bannerResizeObserver = null;
      return;
    }

    const inner = b.querySelector('.consent-banner-inner');
    const bannerStyle = inner ? getComputedStyle(b) : null;
    const pad =
      (bannerStyle ? parseFloat(bannerStyle.paddingBottom) + parseFloat(bannerStyle.paddingTop) : 0) || 16;
    const height = inner ? Math.ceil(inner.getBoundingClientRect().height + pad) : 176;

    document.body.classList.add('consent-banner-visible');
    document.body.style.setProperty('--consent-banner-offset', `${height}px`);

    if (inner && !bannerResizeObserver) {
      bannerResizeObserver = new ResizeObserver(() => updateBannerOffset());
      bannerResizeObserver.observe(inner);
    }
  }

  function hideBanner() {
    const b = document.getElementById('consentBanner');
    if (b) b.hidden = true;
    updateBannerOffset();
    window.dispatchEvent(new CustomEvent('consent-banner-closed'));
    emitConsentSettled();
  }

  function showBanner() {
    const b = document.getElementById('consentBanner');
    if (b) b.hidden = false;
    requestAnimationFrame(updateBannerOffset);
  }

  window.isConsentBannerVisible = function isConsentBannerVisible() {
    const b = document.getElementById('consentBanner');
    return Boolean(b && !b.hidden);
  };

  window.isConsentSettingsVisible = function isConsentSettingsVisible() {
    const s = document.getElementById('consentSettings');
    return Boolean(s && !s.hidden);
  };

  function isConsentUiBlocking() {
    return window.isConsentBannerVisible() || window.isConsentSettingsVisible();
  }

  function emitConsentSettled() {
    if (!isConsentUiBlocking()) {
      window.dispatchEvent(new CustomEvent('consent-settled'));
    }
  }

  /**
   * Callback wenn Cookie-Banner und Einstellungen-Dialog geschlossen sind.
   * @param {Function} callback
   * @param {number} delayMs Pause danach (z. B. bevor Neuigkeiten-Popup)
   */
  window.whenConsentSettled = function whenConsentSettled(callback, delayMs = 500) {
    const run = () => window.setTimeout(callback, delayMs);

    const tryRun = () => {
      if (!isConsentUiBlocking()) run();
    };

    if (!isConsentUiBlocking()) {
      tryRun();
      return;
    }

    window.addEventListener('consent-settled', tryRun, { once: true });
    window.addEventListener('consent-updated', tryRun, { once: true });
  };

  const FLOW_MS = 620;

  function hideSettings() {
    const s = document.getElementById('consentSettings');
    if (!s) return;
    s.classList.remove('is-visible');
    window.setTimeout(() => {
      s.hidden = true;
      document.body.classList.remove('consent-settings-open');
      emitConsentSettled();
    }, FLOW_MS);
  }

  function showSettings() {
    const s = document.getElementById('consentSettings');
    if (s) {
      syncToggleInputs(getConsent() || { external: false });
      s.hidden = false;
      document.body.classList.add('consent-settings-open');
      requestAnimationFrame(() => {
        s.classList.add('is-visible');
        s.querySelector('.consent-settings-panel')?.focus?.();
      });
    }
  }

  function tr(key) {
    const v = window.ktI18n?.t(key);
    return v != null ? v : '';
  }

  function consentMarkup() {
    const close = tr('btn.close') || 'Schließen';
    return `
      <div id="consentBanner" class="consent-banner" role="region" aria-labelledby="consentBannerTitle" hidden>
        <div class="consent-banner-inner card">
          <h2 id="consentBannerTitle">${tr('consent.banner.title')}</h2>
          <p class="consent-lead">${tr('consent.lead')}</p>
          <ul class="consent-list">${tr('consent.list')}</ul>
          <p class="note">${tr('consent.note')}</p>
          <div class="consent-actions">
            <button type="button" class="btn primary" data-consent-all>${tr('consent.acceptAll')}</button>
            <button type="button" class="btn outline" data-consent-essential>${tr('consent.essential')}</button>
            <button type="button" class="btn outline" data-consent-settings>${tr('consent.settings')}</button>
          </div>
        </div>
      </div>
      <div id="consentSettings" class="consent-settings" role="dialog" aria-modal="true" aria-labelledby="consentSettingsTitle" hidden>
        <div class="consent-settings-backdrop" data-consent-close-settings></div>
        <div class="consent-settings-panel card" tabindex="-1">
          <button type="button" class="consent-settings-close" data-consent-close-settings aria-label="${close}">&times;</button>
          <h2 id="consentSettingsTitle">${tr('consent.settingsTitle')}</h2>
          <div class="consent-option">
            <label><input type="checkbox" checked disabled> ${tr('consent.necessary')}</label>
            <p class="note">${tr('consent.necessaryNote')}</p>
          </div>
          <div class="consent-option">
            <label><input type="checkbox" id="consentExternal"> ${tr('consent.external')}</label>
            <p class="note">${tr('consent.externalNote')}</p>
          </div>
          <div class="consent-actions">
            <button type="button" class="btn primary" data-consent-save>${tr('consent.save')}</button>
          </div>
          <p class="note" style="margin-top:1rem">${tr('consent.legalLinks')}</p>
        </div>
      </div>
    `;
  }

  function applyConsentI18n() {
    const title = document.getElementById('consentBannerTitle');
    if (!title) return;

    const close = tr('btn.close') || 'Schließen';
    const setHtml = (sel, key) => {
      const el = document.querySelector(sel);
      const val = tr(key);
      if (el && val != null) el.innerHTML = val;
    };
    const setText = (sel, key) => {
      const el = document.querySelector(sel);
      const val = tr(key);
      if (el && val != null) el.textContent = val;
    };

    setText('#consentBannerTitle', 'consent.banner.title');
    setHtml('#consentBanner .consent-lead', 'consent.lead');
    setHtml('#consentBanner .consent-list', 'consent.list');
    setHtml('#consentBanner .consent-banner-inner > .note', 'consent.note');
    setText('[data-consent-all]', 'consent.acceptAll');
    setText('[data-consent-essential]', 'consent.essential');
    setText('[data-consent-settings]', 'consent.settings');
    setText('#consentSettingsTitle', 'consent.settingsTitle');
    setText('#consentSettings .consent-option:nth-of-type(1) label', 'consent.necessary');
    setHtml('#consentSettings .consent-option:nth-of-type(1) .note', 'consent.necessaryNote');
    setText('#consentSettings .consent-option:nth-of-type(2) label', 'consent.external');
    setHtml('#consentSettings .consent-option:nth-of-type(2) .note', 'consent.externalNote');
    setText('[data-consent-save]', 'consent.save');
    setHtml('#consentSettings .consent-settings-panel > .note', 'consent.legalLinks');

    const closeBtn = document.querySelector('.consent-settings-close');
    if (closeBtn) closeBtn.setAttribute('aria-label', close);
  }

  function bindConsentUi(wrap) {
    wrap.querySelector('[data-consent-all]')?.addEventListener('click', () => {
      saveConsent({ external: true });
    });
    wrap.querySelector('[data-consent-essential]')?.addEventListener('click', () => {
      saveConsent({ external: false });
    });
    wrap.querySelector('[data-consent-settings]')?.addEventListener('click', showSettings);
    wrap.querySelector('[data-consent-save]')?.addEventListener('click', () => {
      saveConsent({ external: document.getElementById('consentExternal')?.checked || false });
    });
    wrap.querySelectorAll('[data-consent-close-settings]').forEach((el) => {
      el.addEventListener('click', hideSettings);
    });
  }

  function injectUi() {
    if (document.getElementById('consentBanner')) return;

    const wrap = document.createElement('div');
    wrap.innerHTML = consentMarkup();
    document.body.appendChild(wrap);

    bindConsentUi(wrap);

    document.addEventListener('click', (e) => {
      if (e.target.matches('[data-enable-maps]')) {
        saveConsent({ external: true });
      }
    });
  }

  function injectFooterLink() {
    document.querySelectorAll('.legal-footer').forEach((footer) => {
      if (footer.querySelector('[data-cookie-settings]')) return;
      const sep = footer.textContent.trim().endsWith('·') ? ' ' : ' · ';
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = 'footer-link-btn';
      btn.dataset.cookieSettings = '1';
      btn.textContent = tr('consent.footerLink') || 'Cookie-Einstellungen';
      btn.addEventListener('click', (e) => {
        e.preventDefault();
        showSettings();
      });
      footer.appendChild(document.createTextNode(sep));
      footer.appendChild(btn);
    });
  }

  function init() {
    injectUi();
    injectFooterLink();

    const existing = getConsent();
    if (existing) {
      applyConsent(existing);
      hideBanner();
    } else {
      showBanner();
    }

    window.__consentReady = true;
    window.dispatchEvent(
      new CustomEvent('consent-ready', {
        detail: existing || { necessary: true, external: false },
      })
    );
  }

  document.addEventListener('kt-lang-change', () => {
    if (document.getElementById('consentBanner')) applyConsentI18n();
    document.querySelectorAll('[data-cookie-settings]').forEach((btn) => {
      btn.textContent = tr('consent.footerLink') || 'Cookie-Einstellungen';
    });
    updateBannerOffset();
  });

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();

;
/**
 * Barrierefreiheits-Einstellungen (WCAG 2.2 / EN 301 549).
 * Präferenzen werden in localStorage gespeichert.
 */
(function () {
  if (document.body.classList.contains('admin-app')) return;

  const STORAGE_KEY = 'kunsttherapie-a11y';
  const root = document.documentElement;

  const defaults = {
    textSize: 'normal',
    contrast: 'default',
    motion: 'default',
    links: 'default',
    spacing: 'default',
    focus: 'default',
  };

  function load() {
    try {
      const raw = localStorage.getItem(STORAGE_KEY);
      return raw ? { ...defaults, ...JSON.parse(raw) } : { ...defaults };
    } catch {
      return { ...defaults };
    }
  }

  function save(prefs) {
    try {
      localStorage.setItem(STORAGE_KEY, JSON.stringify(prefs));
    } catch {
      /* ignore */
    }
  }

  function apply(prefs) {
    root.dataset.a11yText = prefs.textSize;
    root.dataset.a11yContrast = prefs.contrast;
    root.dataset.a11yMotion = prefs.motion;
    root.dataset.a11yLinks = prefs.links;
    root.dataset.a11ySpacing = prefs.spacing;
    root.dataset.a11yFocus = prefs.focus;
    root.classList.toggle('a11y-reduce-motion', prefs.motion === 'reduce');
    root.classList.toggle('a11y-high-contrast', prefs.contrast === 'high');
  }

  function tr(key) {
    const v = window.ktI18n?.t(key);
    return v != null ? v : '';
  }

  let prefs = load();
  apply(prefs);

  const panelId = 'a11yPanel';
  const toggleId = 'a11yToggle';

  function panelHtml() {
    const closeLabel = tr('a11y.close') || tr('btn.close') || 'Schließen';
    return `
    <header class="a11y-panel-head">
      <h2 id="a11yPanelTitle">${tr('a11y.title')}</h2>
      <button type="button" class="a11y-panel-close" data-a11y-close aria-label="${closeLabel}">×</button>
    </header>
    <p class="a11y-panel-intro">${tr('a11y.intro')}</p>
    <form class="a11y-form" id="a11yForm">
      <fieldset>
        <legend>${tr('a11y.fontSize')}</legend>
        <label><input type="radio" name="textSize" value="normal"/> ${tr('a11y.font.normal')}</label>
        <label><input type="radio" name="textSize" value="large"/> ${tr('a11y.font.large')}</label>
        <label><input type="radio" name="textSize" value="xlarge"/> ${tr('a11y.font.xlarge')}</label>
      </fieldset>
      <fieldset>
        <legend>${tr('a11y.contrast')}</legend>
        <label><input type="radio" name="contrast" value="default"/> ${tr('a11y.contrast.default')}</label>
        <label><input type="radio" name="contrast" value="high"/> ${tr('a11y.contrast.high')}</label>
      </fieldset>
      <fieldset>
        <legend>${tr('a11y.motion')}</legend>
        <label><input type="radio" name="motion" value="default"/> ${tr('a11y.motion.default')}</label>
        <label><input type="radio" name="motion" value="reduce"/> ${tr('a11y.motion.reduce')}</label>
      </fieldset>
      <fieldset>
        <legend>${tr('a11y.links')}</legend>
        <label><input type="radio" name="links" value="default"/> ${tr('a11y.links.default')}</label>
        <label><input type="radio" name="links" value="underline"/> ${tr('a11y.links.underline')}</label>
      </fieldset>
      <fieldset>
        <legend>${tr('a11y.spacing')}</legend>
        <label><input type="radio" name="spacing" value="default"/> ${tr('a11y.spacing.default')}</label>
        <label><input type="radio" name="spacing" value="readable"/> ${tr('a11y.spacing.readable')}</label>
      </fieldset>
      <fieldset>
        <legend>${tr('a11y.focus')}</legend>
        <label><input type="radio" name="focus" value="default"/> ${tr('a11y.focus.default')}</label>
        <label><input type="radio" name="focus" value="strong"/> ${tr('a11y.focus.strong')}</label>
      </fieldset>
      <div class="a11y-actions">
        <button type="button" class="btn outline" data-a11y-reset>${tr('a11y.reset')}</button>
      </div>
    </form>
    <p class="a11y-live" id="a11yLive" role="status" aria-live="polite" aria-atomic="true"></p>
  `;
  }

  const toggle = document.createElement('button');
  toggle.type = 'button';
  toggle.id = toggleId;
  toggle.className = 'a11y-fab';
  toggle.setAttribute('aria-expanded', 'false');
  toggle.setAttribute('aria-controls', panelId);
  toggle.innerHTML =
    '<span class="a11y-fab-icon" aria-hidden="true"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="6" r="2"/><path d="M12 8v2M7 20l2-6M17 20l-2-6M5 14h14"/></svg></span>';

  const panel = document.createElement('section');
  panel.id = panelId;
  panel.className = 'a11y-panel';
  panel.hidden = true;
  panel.setAttribute('role', 'dialog');
  panel.setAttribute('aria-modal', 'true');
  panel.setAttribute('aria-labelledby', 'a11yPanelTitle');

  function applyToggleLabel() {
    toggle.setAttribute('aria-label', tr('a11y.toggle') || 'Barrierefreiheit');
  }

  applyToggleLabel();
  panel.innerHTML = panelHtml();

  document.body.appendChild(toggle);
  document.body.appendChild(panel);

  let form = panel.querySelector('#a11yForm');
  let live = panel.querySelector('#a11yLive');

  function syncForm() {
    if (!form) return;
    Object.keys(defaults).forEach((key) => {
      const input = form.querySelector(`input[name="${key}"][value="${prefs[key]}"]`);
      if (input) input.checked = true;
    });
  }

  function announce(msg) {
    if (live) live.textContent = msg;
  }

  function onFormChange(e) {
    const target = e.target;
    if (!(target instanceof HTMLInputElement) || target.type !== 'radio') return;
    if (!target.closest('#a11yForm')) return;
    prefs = { ...prefs, [target.name]: target.value };
    save(prefs);
    apply(prefs);
    announce(tr('a11y.announce.set') || 'Einstellung übernommen.');
  }

  function onReset() {
    prefs = { ...defaults };
    save(prefs);
    apply(prefs);
    syncForm();
    announce(tr('a11y.announce.reset') || 'Alle Einstellungen zurückgesetzt.');
  }

  const FLOW_MS = 620;

  function openPanel() {
    panel.hidden = false;
    toggle.setAttribute('aria-expanded', 'true');
    syncForm();
    requestAnimationFrame(() => {
      panel.classList.add('a11y-panel--visible');
      panel.querySelector('[data-a11y-close]')?.focus();
    });
  }

  function closePanel() {
    panel.classList.remove('a11y-panel--visible');
    toggle.setAttribute('aria-expanded', 'false');
    window.setTimeout(() => {
      panel.hidden = true;
      toggle.focus();
    }, FLOW_MS);
  }

  function refreshPanelI18n() {
    const wasOpen = !panel.hidden;
    panel.innerHTML = panelHtml();
    form = panel.querySelector('#a11yForm');
    live = panel.querySelector('#a11yLive');
    applyToggleLabel();
    syncForm();
    panel.hidden = !wasOpen;
    panel.classList.toggle('a11y-panel--visible', wasOpen);
    document.querySelectorAll('[data-a11y-open]').forEach((el) => {
      el.textContent = tr('a11y.footerBtn') || 'Barrierefreiheit';
    });
  }

  toggle.addEventListener('click', () => {
    if (panel.hidden) openPanel();
    else closePanel();
  });

  panel.addEventListener('click', (e) => {
    if (e.target.closest('[data-a11y-close]')) closePanel();
    if (e.target.closest('[data-a11y-reset]')) onReset();
  });

  panel.addEventListener('change', onFormChange);

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && !panel.hidden) {
      e.preventDefault();
      closePanel();
    }
  });

  syncForm();

  document.querySelectorAll('[data-a11y-open]').forEach((el) => {
    el.addEventListener('click', (e) => {
      e.preventDefault();
      openPanel();
    });
  });

  const legalFooter = document.querySelector('.legal-footer');
  if (legalFooter && !legalFooter.querySelector('[data-a11y-open]')) {
    const sep = document.createTextNode(' · ');
    const btn = document.createElement('button');
    btn.type = 'button';
    btn.className = 'a11y-footer-link';
    btn.setAttribute('data-a11y-open', '');
    btn.textContent = tr('a11y.footerBtn') || 'Barrierefreiheit';
    legalFooter.appendChild(sep);
    legalFooter.appendChild(btn);
  }

  if (
    prefs.motion === 'default' &&
    window.matchMedia('(prefers-reduced-motion: reduce)').matches
  ) {
    prefs.motion = 'reduce';
    save(prefs);
    apply(prefs);
    syncForm();
  }

  document.addEventListener('kt-lang-change', refreshPanelI18n);
})();

;
/**
 * Gemeinsame UI: Skip-Link, Mobile-Menü, dynamische Navigation.
 */
(function () {
  if (document.body.classList.contains('admin-app')) return;

  const NAV_PATHS = {
    'ueber-mich': 'ansatz',
    kunsttherapie: 'kunsttherapie',
    buchung: 'termin',
    kontakt: 'kontakt',
    neuigkeiten: 'neuigkeiten',
    events: 'events',
    preise: 'preise',
    atelier: 'atelier',
    impressum: 'impressum',
    datenschutz: 'datenschutz',
    index: 'home',
  };

  /** Startseite: Abschnitt → Nav-Schlüssel */
  const HOME_SECTIONS = [
    { id: 'neuigkeiten', nav: 'neuigkeiten' },
    { id: 'angebote', nav: 'kunsttherapie' },
    { id: 'atelier', nav: 'praxis' },
  ];

  function currentNavKey() {
    const path = window.location.pathname.replace(/\/$/, '');
    if (path === '' || path.endsWith('/index') || path.endsWith('/index.html')) return 'home';
    const base = path.split('/').pop().replace(/\.html$/, '');
    return NAV_PATHS[base] || null;
  }

  function setActiveNav(key) {
    document.querySelectorAll('nav[data-site-nav] a[data-nav]').forEach((link) => {
      const active = Boolean(key && link.dataset.nav === key);
      if (active) {
        link.setAttribute('aria-current', 'page');
      } else {
        link.removeAttribute('aria-current');
      }
    });
  }

  function markCurrentNav() {
    const pageKey = currentNavKey();
    const hash = window.location.hash.replace('#', '');

    if (pageKey === 'kunsttherapie' && (hash === 'atelier' || hash === 'praxis')) {
      setActiveNav('praxis');
      return;
    }

    if (pageKey !== 'home') {
      setActiveNav(pageKey);
      return;
    }

    if (hash) {
      const match = HOME_SECTIONS.find((s) => s.id === hash);
      if (match) {
        setActiveNav(match.nav);
        return;
      }
    }

    updateHomeScrollNav();
  }

  function updateHomeScrollNav() {
    if (currentNavKey() !== 'home') return;

    const headerOffset = 100;
    const scrollPos = window.scrollY + headerOffset;

    if (scrollPos < 180) {
      setActiveNav(null);
      return;
    }

    let activeNav = null;
    HOME_SECTIONS.forEach(({ id, nav }) => {
      const el = document.getElementById(id);
      if (!el) return;
      if (el.offsetTop <= scrollPos) {
        activeNav = nav;
      }
    });

    setActiveNav(activeNav);
  }

  function initHomeScrollSpy() {
    if (currentNavKey() !== 'home') return;

    let ticking = false;
    const onScroll = () => {
      if (ticking) return;
      ticking = true;
      window.requestAnimationFrame(() => {
        updateHomeScrollNav();
        ticking = false;
      });
    };

    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('hashchange', markCurrentNav);
  }

  const CHROME_NAV = [
    ['ansatz', 'nav.ansatz'],
    ['kunsttherapie', 'nav.therapy'],
    ['praxis', 'nav.practice'],
    ['neuigkeiten', 'nav.news'],
    ['termin', 'nav.booking'],
    ['kontakt', 'nav.contact'],
  ];

  const CHROME_FOOTER = [
    ['.site-footer .footer-col:nth-child(1) h4', 'footer.practice'],
    ['.site-footer .footer-col:nth-child(1) p:nth-of-type(2)', 'footer.role'],
    ['.site-footer .footer-col:nth-child(2) h4', 'footer.contact'],
    ['.site-footer .footer-col:nth-child(3) h4', 'footer.offer'],
    ['.site-footer .footer-col:nth-child(3) a[href*="kunsttherapie"]', 'nav.therapy'],
    ['.site-footer .footer-col:nth-child(3) a[href*="buchung"]', 'btn.book'],
    ['.site-footer .footer-col:nth-child(3) a[href*="events"]', 'nav.events'],
    ['.site-footer .footer-col:nth-child(3) a[href*="neuigkeiten"]', 'nav.news'],
    ['.site-footer .footer-col:nth-child(3) a[href*="preise"]', 'footer.prices'],
    ['.site-footer .footer-col:nth-child(3) a[href*="atelier"]', 'footer.atelier'],
    ['.site-footer .footer-col:nth-child(4) h4', 'footer.legal'],
    ['.site-footer .footer-col:nth-child(4) a[href*="impressum"]', 'footer.imprint'],
    ['.site-footer .footer-col:nth-child(4) a[href*="datenschutz"]', 'footer.privacy'],
  ];

  function applySiteChrome() {
    if (!window.ktI18n) return;
    const t = window.ktI18n.t.bind(window.ktI18n);

    document.querySelectorAll('.skip-link').forEach((el) => {
      const v = t('a11y.skip');
      if (v) el.textContent = v;
    });

    document.querySelectorAll('.brand.logo-text .title').forEach((el) => {
      const v = t('brand.subtitle');
      if (v) el.textContent = v;
    });

    document.querySelectorAll('nav[data-site-nav]').forEach((nav) => {
      const label = t('nav.aria');
      if (label) nav.setAttribute('aria-label', label);
    });

    function setI18nText(el, key) {
      const v = t(key);
      if (v == null) return;
      const inner = el.querySelector(':scope > [data-i18n]');
      if (inner) inner.textContent = v;
      else if (!el.hasAttribute('data-i18n')) el.textContent = v;
    }

    CHROME_NAV.forEach(([navKey, i18nKey]) => {
      document.querySelectorAll(`nav[data-site-nav] a[data-nav="${navKey}"]`).forEach((link) => {
        if (link.hasAttribute('data-i18n')) return;
        setI18nText(link, i18nKey);
      });
    });

    CHROME_FOOTER.forEach(([sel, key]) => {
      document.querySelectorAll(sel).forEach((el) => {
        if (el.hasAttribute('data-i18n')) return;
        setI18nText(el, key);
      });
    });

    document.querySelectorAll('.nav-toggle-label').forEach((el) => {
      const v = t('nav.menu');
      if (v) el.textContent = v;
    });

    document.querySelectorAll('.legal-footer').forEach((el) => {
      const tag = t('footer.tagline');
      if (!tag) return;

      const hasExtra =
        el.querySelector(
          '[data-cookie-settings], [data-a11y-open], .footer-link-btn, .a11y-footer-link'
        ) != null;

      if (!hasExtra) {
        const year = String(new Date().getFullYear());
        el.innerHTML = `&copy; <span id="y">${year}</span><span class="footer-tagline-text"> ${tag}</span>`;
        return;
      }

      let yearEl = el.querySelector('#y, #year');
      if (!yearEl) {
        yearEl = document.createElement('span');
        yearEl.id = 'y';
        el.prepend(yearEl);
        if (!el.textContent.trim().startsWith('©')) {
          el.prepend(document.createTextNode('© '));
        }
      }
      if (!yearEl.textContent?.trim()) {
        yearEl.textContent = String(new Date().getFullYear());
      }

      el.querySelectorAll('[data-i18n="footer.tagline"]').forEach((node) => node.remove());

      let taglineEl = el.querySelector('.footer-tagline-text');
      if (!taglineEl) {
        taglineEl = document.createElement('span');
        taglineEl.className = 'footer-tagline-text';
        const insertBefore = el.querySelector(
          '[data-cookie-settings], [data-a11y-open], .footer-link-btn, .a11y-footer-link'
        );
        el.insertBefore(taglineEl, insertBefore || null);
      }
      taglineEl.textContent = ` ${tag}`;
    });
  }

  window.ktApplySiteChrome = applySiteChrome;

  function initEmailLinks() {
    document.querySelectorAll('.js-email[data-u][data-d]').forEach((link) => {
      const user = link.getAttribute('data-u');
      const domain = link.getAttribute('data-d');
      if (!user || !domain) return;
      const addr = `${user}@${domain}`;
      link.href = `mailto:${addr}`;
      if (!link.hasAttribute('data-i18n') && !link.textContent.trim()) {
        link.textContent = addr;
      }
    });
  }

  window.ktInitEmailLinks = initEmailLinks;

  function initSkipLink() {
    const existing = document.querySelector('.skip-link');
    if (existing) {
      if (!existing.hasAttribute('data-i18n')) existing.setAttribute('data-i18n', 'a11y.skip');
      return;
    }
    const main = document.querySelector('main');
    if (!main) return;
    if (!main.id) main.id = 'main';

    const skip = document.createElement('a');
    skip.href = '#main';
    skip.className = 'skip-link';
    skip.setAttribute('data-i18n', 'a11y.skip');
    skip.textContent = 'Zum Inhalt springen';
    document.body.prepend(skip);
  }

  function initMobileNav() {
    const header = document.querySelector('header .header-inner');
    const nav = document.querySelector('nav[data-site-nav]');
    if (!header || !nav || header.querySelector('.nav-toggle')) return;

    const toggle = document.createElement('button');
    toggle.type = 'button';
    toggle.className = 'nav-toggle';
    toggle.setAttribute('aria-expanded', 'false');
    toggle.setAttribute('aria-controls', 'site-nav');
    toggle.innerHTML =
      '<span class="nav-toggle-bar" aria-hidden="true"></span><span class="nav-toggle-label">Menü</span>';

    const navId = nav.id || 'site-nav';
    nav.id = navId;
    toggle.setAttribute('aria-controls', navId);

    const tools = header.querySelector('.header-tools');
    if (tools) {
      tools.appendChild(toggle);
    } else {
      header.insertBefore(toggle, nav);
    }

    toggle.addEventListener('click', () => {
      const open = document.body.classList.toggle('nav-open');
      toggle.setAttribute('aria-expanded', open ? 'true' : 'false');
    });

    nav.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => {
        document.body.classList.remove('nav-open');
        toggle.setAttribute('aria-expanded', 'false');
      });
    });

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && document.body.classList.contains('nav-open')) {
        document.body.classList.remove('nav-open');
        toggle.setAttribute('aria-expanded', 'false');
        toggle.focus();
      }
    });
  }

  function setFooterYear() {
    const y = String(new Date().getFullYear());
    document.querySelectorAll('#y, #year').forEach((el) => {
      el.textContent = y;
    });
  }

  function bindLangSwitchButtons(wrap) {
    wrap.querySelectorAll('.lang-switch-btn[data-lang]').forEach((btn) => {
      if (btn.dataset.langBound === '1') return;
      btn.dataset.langBound = '1';
      btn.addEventListener('click', () => {
        if (window.ktI18n) window.ktI18n.setLang(btn.dataset.lang);
      });
    });
  }

  function initLangSwitch() {
    const header = document.querySelector('header .header-inner');
    if (!header) return;

    let wrap = header.querySelector('.lang-switch');
    if (wrap) {
      bindLangSwitchButtons(wrap);
      const label = window.ktI18n?.t('lang.switch');
      if (label) wrap.setAttribute('aria-label', label);
      return;
    }

    wrap = document.createElement('div');
    wrap.className = 'lang-switch';
    wrap.setAttribute('role', 'group');
    wrap.setAttribute('aria-label', window.ktI18n?.t('lang.switch') || 'Sprache');

    const btns = document.createElement('div');
    btns.className = 'lang-switch-btns';

    ['de', 'en'].forEach((code) => {
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = 'lang-switch-btn';
      btn.dataset.lang = code;
      btn.textContent = code.toUpperCase();
      btns.appendChild(btn);
    });

    wrap.appendChild(btns);
    bindLangSwitchButtons(wrap);

    const tools = header.querySelector('.header-tools');
    const nav = header.querySelector('nav[data-site-nav]');
    if (tools) {
      tools.prepend(wrap);
    } else if (nav) {
      header.insertBefore(wrap, nav);
    } else {
      header.appendChild(wrap);
    }
  }

  window.ktInitLangSwitch = initLangSwitch;

  function redirectLegacyPraxisHash() {
    if (window.location.hash === '#praxis') {
      history.replaceState(null, '', `${window.location.pathname}${window.location.search}#atelier`);
    }
  }

  function init() {
    redirectLegacyPraxisHash();
    initSkipLink();
    initEmailLinks();
    initMobileNav();
    initLangSwitch();
    applySiteChrome();
    markCurrentNav();
    initHomeScrollSpy();
    setFooterYear();
  }

  document.addEventListener('kt-lang-change', () => {
    applySiteChrome();
    initEmailLinks();
    setFooterYear();
    const group = document.querySelector('.lang-switch');
    if (group && window.ktI18n) {
      group.setAttribute('aria-label', window.ktI18n.t('lang.switch') || 'Sprache');
    }
  });

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();

;
/**
 * Wendet Admin-Bild-Overrides auf [data-site-image] an (Fallback: HTML-Standard).
 * Steuert außerdem, wie viele Galerie-Bilder auf der Startseite sichtbar sind.
 */
(function () {
  if (document.body.classList.contains('admin-app')) return;

  const GALLERY_LAYOUT_CLASSES = [
    'image-gallery--duo',
    'image-gallery--trio',
    'image-gallery--six',
  ];

  function applyToElement(el, url) {
    if (!url) return;

    if (el.tagName === 'IMG') {
      el.src = url;
      const picture = el.closest('picture');
      if (picture) {
        picture.querySelectorAll('source').forEach((source) => source.remove());
      }
      return;
    }

    const img = el.querySelector('img');
    if (img) applyToElement(img, url);
  }

  function applyGalleryCount(count) {
    const gallery = document.querySelector('[data-gallery]');
    if (!gallery) return;

    const safe = Math.min(6, Math.max(1, Number.parseInt(count, 10) || 6));
    const items = [...gallery.querySelectorAll('[data-gallery-open]')];

    items.forEach((item, i) => {
      const show = i < safe;
      item.hidden = !show;
    });

    items.forEach((item) => item.classList.remove('gallery-item--wide'));

    GALLERY_LAYOUT_CLASSES.forEach((cls) => gallery.classList.remove(cls));

    if (safe <= 2) gallery.classList.add('image-gallery--duo');
    else if (safe === 3) gallery.classList.add('image-gallery--trio');
    else if (safe >= 5) gallery.classList.add('image-gallery--six');

    const lastVisible = items[safe - 1];
    if (lastVisible && safe === 5) {
      lastVisible.classList.add('gallery-item--wide');
    }

    gallery.dataset.galleryCount = String(safe);
    document.dispatchEvent(new CustomEvent('kt-gallery-count', { detail: { count: safe } }));
  }

  window.ktApplyGalleryCount = applyGalleryCount;

  async function applySiteImages() {
    try {
      const res = await fetch('/api/site-images', { credentials: 'same-origin' });
      if (!res.ok) return;
      const data = await res.json();
      const images = data.images || {};

      document.querySelectorAll('[data-site-image]').forEach((el) => {
        const slot = el.getAttribute('data-site-image');
        const url = images[slot];
        if (!url) return;
        applyToElement(el, url);
      });

      if (data.galleryCount != null) {
        applyGalleryCount(data.galleryCount);
      }
    } catch (_) {
      /* statisches Hosting ohne API */
    }
  }

  function initGalleryLayout() {
    const gallery = document.querySelector('[data-gallery]');
    if (!gallery) return;
    const preset = gallery.dataset.galleryCount;
    applyGalleryCount(preset ? Number(preset) : 6);
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', () => {
      initGalleryLayout();
      applySiteImages();
    });
  } else {
    initGalleryLayout();
    applySiteImages();
  }
})();

;
/**
 * Sequenzielles Einblenden: Elemente erscheinen nacheinander (staggered reveal).
 */
(function () {
  if (document.body.classList.contains('admin-app')) return;

  const STEP_MS = 88;
  const HEADER_STEP_MS = 48;
  const SECTION_GAP_MS = 140;
  const INITIAL_DELAY_MS = 100;
  const SKIP_SELECTOR =
    '.consent-banner, .consent-settings, #newsPopup, .lightbox, [hidden], script, style, noscript';

  const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  let queue = Promise.resolve();

  function isSkipped(el) {
    if (!el || !(el instanceof Element)) return true;
    if (el.matches(SKIP_SELECTOR)) return true;
    if (el.closest(SKIP_SELECTOR)) return true;
    if (el.closest('[data-reveal-skip]')) return true;
    if (el.offsetParent === null) return true;
    return false;
  }

  function collectSectionBlocks(section, add, seen) {
    if (section.hidden) return;

    const intro = section.querySelector(':scope > .section-intro, :scope > .page-hero');
    if (intro) {
      [...intro.children].forEach((child) => add(child));
    }

    const heroGrid = section.querySelector(':scope > .hero-grid');
    if (heroGrid) {
      [...heroGrid.children].forEach((child) => add(child));
    }

    if (section.classList.contains('kt-hero')) {
      section.querySelectorAll(':scope > .card').forEach((el) => add(el));
    }

    section
      .querySelectorAll(
        ':scope > .services-grid > .card, :scope > .image-gallery > .gallery-item, :scope > .kt-offers > [data-offer], :scope > .card.hero-left, :scope > .card.canvasWrap, :scope > .booking-layout > .card, :scope > .booking-form-panel, :scope > .twoCol > div'
      )
      .forEach((el) => add(el));

    section.querySelectorAll(':scope > .center').forEach((el) => add(el));

    section.querySelectorAll(':scope > .quote-showcase').forEach((el) => add(el));

    section.querySelectorAll(':scope > .card').forEach((el) => add(el));
  }

  function collectFlatMain(main, add, seen) {
    main.querySelectorAll('.page-hero > *, .section-intro > *').forEach((el) => add(el));
    main.querySelectorAll('.booking-layout > .card, .booking-form-panel, :scope > .card').forEach((el) => {
      add(el);
    });
  }

  function collectPageTargets() {
    const seen = new Set();
    const items = [];

    function add(el) {
      if (isSkipped(el) || seen.has(el)) return;
      seen.add(el);
      items.push(el);
    }

    const brand = document.querySelector('header .brand');
    if (brand) add(brand);
    document.querySelectorAll('header nav li').forEach((el) => add(el));

    const main = document.querySelector('main');
    if (main) {
      const sections = main.querySelectorAll(':scope > section');
      if (sections.length) {
        sections.forEach((section) => collectSectionBlocks(section, add, seen));
      } else {
        collectFlatMain(main, add, seen);
        const heroGrid = main.querySelector(':scope > .hero-grid');
        if (heroGrid) {
          [...heroGrid.children].forEach((child) => add(child));
        }
        main.querySelectorAll(':scope > .card').forEach((el) => add(el));
      }
    }

    document.querySelectorAll('.site-footer .footer-col').forEach((el) => add(el));
    const legal = document.querySelector('.site-footer .legal-footer');
    if (legal) add(legal);

    return items;
  }

  function prepareElement(el) {
    if (el.classList.contains('reveal-item')) return;
    el.classList.add('reveal-item');
  }

  function revealElement(el) {
    el.classList.add('reveal-visible');
  }

  function runSequence(elements, startDelay = 0) {
    if (!elements.length) return Promise.resolve();

    if (reducedMotion) {
      elements.forEach((el) => {
        prepareElement(el);
        revealElement(el);
      });
      return Promise.resolve();
    }

    return new Promise((resolve) => {
      let delay = startDelay;
      let prevSection = null;

      elements.forEach((el) => {
        const section = el.closest('main > section');
        if (prevSection && section && section !== prevSection) {
          delay += SECTION_GAP_MS;
        }
        if (section) prevSection = section;

        prepareElement(el);
        const at = delay;
        window.setTimeout(() => revealElement(el), at);
        delay += el.closest('header') ? HEADER_STEP_MS : STEP_MS;
      });

      window.setTimeout(resolve, delay + 200);
    });
  }

  function revealStagger(nodes, options = {}) {
    const list = [...nodes].filter((el) => el && !isSkipped(el));
    if (!list.length) return Promise.resolve();

    const task = () => runSequence(list, options.delay || 0);
    queue = queue.then(task);
    return queue;
  }

  function initPage() {
    const targets = collectPageTargets();
    if (!targets.length) return;

    document.documentElement.classList.add('reveal-active');

    if (reducedMotion) {
      targets.forEach((el) => {
        prepareElement(el);
        revealElement(el);
      });
      return;
    }

    requestAnimationFrame(() => {
      targets.forEach((el) => prepareElement(el));
      requestAnimationFrame(() => {
        runSequence(targets, INITIAL_DELAY_MS);
      });
    });
  }

  window.revealStagger = revealStagger;
  window.whenRevealIdle = function whenRevealIdle() {
    return queue;
  };

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initPage);
  } else {
    initPage();
  }
})();

;
/**
 * Künstlerischer „Nach oben“-Button – erscheint beim Scrollen.
 */
(function () {
  if (document.body.classList.contains('admin-app')) return;

  const SHOW_AFTER_PX = 380;
  let btn = null;

  function createButton() {
    btn = document.createElement('button');
    btn.type = 'button';
    btn.className = 'scroll-top-btn';
    btn.hidden = true;
    const label = window.ktI18n?.t('ui.scrollTop') || 'Nach oben scrollen';
    btn.setAttribute('aria-label', label);
    btn.dataset.i18nAria = 'ui.scrollTop';
    btn.innerHTML = '<span class="scroll-top-arrow" aria-hidden="true">↑</span>';
    btn.addEventListener('click', () => {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });
    document.body.appendChild(btn);
  }

  function updateBottomOffset() {
    if (!btn) return;
    const bannerVisible =
      typeof window.isConsentBannerVisible === 'function' && window.isConsentBannerVisible();
    btn.style.setProperty('--scroll-top-bottom', bannerVisible ? '6.5rem' : 'clamp(1rem, 3vw, 1.75rem)');
  }

  function updateVisibility() {
    if (!btn) return;
    updateBottomOffset();
    const show = window.scrollY > SHOW_AFTER_PX;
    if (show && btn.hidden) {
      btn.hidden = false;
      requestAnimationFrame(() => btn.classList.add('scroll-top-visible'));
    } else if (!show && !btn.hidden) {
      btn.classList.remove('scroll-top-visible');
      window.setTimeout(() => {
        if (window.scrollY <= SHOW_AFTER_PX) btn.hidden = true;
      }, 620);
    }
  }

  function init() {
    createButton();
    updateVisibility();
    window.addEventListener('scroll', updateVisibility, { passive: true });
    window.addEventListener('consent-settled', updateBottomOffset);
    window.addEventListener('consent-updated', updateBottomOffset);
    window.addEventListener('consent-banner-closed', updateBottomOffset);
  }

  document.addEventListener('kt-lang-change', () => {
    if (!btn) return;
    const label = window.ktI18n?.t('ui.scrollTop') || 'Nach oben scrollen';
    btn.setAttribute('aria-label', label);
  });

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
